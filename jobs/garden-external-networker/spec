---
name: garden-external-networker
templates:
  ext-plugin_ctl.erb: bin/ext-plugin_ctl

packages:
  - runc-cni

properties:
  disable:
    description: "Disable container to container networking."
    default: false
  config_file:
    description: "Config file for the garden-external-networker"
    default: "/var/vcap/jobs/garden-cni/config/adapter.json"
  socket:
    description: "Socket for communication with the garden server"
    default: "/var/vcap/data/garden/tmp/netplugin-shim.sock"
